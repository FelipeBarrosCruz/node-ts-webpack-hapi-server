'use strict';

const should = require('chai').should();

describe('Setup Application', () => {
  
  it('Should run application', function (done) {
    let application = null;
    (application === null).should.equal(true);
    return done(null);
  });
});